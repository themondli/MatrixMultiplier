# MatrixMultiplier

A Python project for performing matrix multiplication with multiple approaches. This includes:

Standard nested-loop multiplication
Optimized techniques for faster computation
Handling matrices of different sizes (where multiplication is valid)
Requirements
Python 3.x
NumPy (optional, for optimized operations)

Usage
Run the script and provide two matrices
